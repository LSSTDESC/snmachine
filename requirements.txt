# Requires Python >= 3.10 (the ELAsTiCC loader uses 3.10+ syntax). Tested with Python 3.12.

###### Core ######
numpy>=1.26,<2  # 1.26 is the first release for Python 3.12; snmachine is untested on numpy 2
scipy>=1.11,<1.14  # scipy.integrate.trapz (used in snclassifier) was removed in 1.14
pandas>=2.1,<3  # pandas 3 copy-on-write breaks chained assignment in snaugment
astropy>=6.0,<7.1
matplotlib>=3.0.0
scikit-learn
lightgbm
george>=0.4.4
iminuit
pywavelets>=1.4
sncosmo>=2.1.0
seaborn
pyyaml>=3.13
future>=1.0  # older versions don't work on Python 3.12
tqdm  # ELAsTiCC loader
setuptools<81  # snmachine/__init__.py imports pkg_resources
setuptools_scm

###### Notebooks ######
jupyterlab
ipykernel
ipympl

###### Tests ######
pytest
pytest-mpl
